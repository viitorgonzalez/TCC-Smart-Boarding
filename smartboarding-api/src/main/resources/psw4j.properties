# Configuração do password4j — desabilita warnings de default
# Os valores abaixo são ignorados pois passamos os parâmetros diretamente
# via Argon2Function.getInstance() no PepperedArgon2PasswordEncoder
global.salt.length=16
