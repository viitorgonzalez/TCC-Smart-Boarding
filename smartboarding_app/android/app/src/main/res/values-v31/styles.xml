<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ ignora o windowBackground do LaunchTheme e usa a SplashScreen
         API do sistema: fundo solido + icone mascarado num CIRCULO. Sem declarar
         aqui, a abertura piscava o branco padrao em vez do fundo da marca. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
